apiVersion: v1
kind: Service
metadata:
  name: mysql
  labels:
    app: mysql
spec:
  type: {{ .Values.service.type }}
  selector:
    app: mysql
  ports:
  - name: mysql
    port: {{ .Values.service.port }}
    targetPort: 3306
